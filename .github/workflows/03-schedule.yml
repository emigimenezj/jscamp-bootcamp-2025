name: 03 - Schedule

on:
  schedule:
    # Minuto (0), hora (9), día del mes (*), mes (*), día de la semana (1 = lunes).
    - cron: "0 9 * * 1"
  workflow_dispatch:

jobs:
  saludar:
    runs-on: ubuntu-24.04
    steps:
      - name: Saludar
        run: echo "Hola desde GitHub Actions!"
