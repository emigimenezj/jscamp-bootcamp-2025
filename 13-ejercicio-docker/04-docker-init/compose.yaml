# Compose simplifica correr el servicio con `docker compose up`
# (equivalente a docker build + docker run con sus flags)
services:
  server:
    build:
      context: .
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"
